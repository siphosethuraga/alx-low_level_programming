#include <stdio.h>

/**
 * main - prints all string 0 to 9
 * Return: Always 0 (success)
 */
int main(void)
{
	int numb;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);

}
