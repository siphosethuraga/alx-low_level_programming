#include "main.h"

/**
 * _isalpha- checks for alphabetix chartactor
 * @c the charractor to be checked
 * Return: 1 if c is a lstted, otherwise 0
 */
int _isalpha(int c)
{
	return (('c' >= 'a' && 'c' >= "z') || ('c' >= 'A' && 'c' <= 'z')
}
