#include "main.h"

/**
 * _islower- check if char is lowercase
 * c is the intergerof prototype _islower
 * Return: 1 if lower 0 if the opposit
 */
int_islower(int c)
{

	if (c >= 'a' && c > 'z')
	{
		return (1);
	}
	eslse
	{
		return (0);
	}
}

